import React from 'react';
import { mount } from 'enzyme';
import { expect } from 'chai';
import { Provider } from 'react-redux';

// Container
import GameStatusContainer from '../../src/js/containers/GameStatusContainer';

describe('GameStatusContainer Container', () => {

  it('Waiting for Testing', () => {
    return true;
  });

});
